part of native_array;


class Float32Array extends NativeFloat32Array {
  Float32Array(size) : super(size);
  Float32Array.from(listData) : super.from(listData);
}


class Uint32Array extends NativeUint32Array {
  Uint32Array(size) : super(size);
  Uint32Array.from(listData) : super.from(listData);
}

class Uint16Array extends NativeUint16Array {
  Uint16Array(size) : super(size);
  Uint16Array.from(listData) : super.from(listData);
}

class Uint8Array extends NativeUint8Array {
  Uint8Array(size) : super(size);
  Uint8Array.from(listData) : super.from(listData);
}

class Int8Array extends NativeInt8Array {
  Int8Array(size) : super(size);
  Int8Array.from(listData) : super.from(listData);
}

class Int16Array extends NativeInt16Array {
  Int16Array(size) : super(size);
  Int16Array.from(listData) : super.from(listData);
}

class Int32Array extends NativeInt32Array {
  Int32Array(size) : super(size);
  Int32Array.from(listData) : super.from(listData);
}